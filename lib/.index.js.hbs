/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const versionup = require('./versionup.js')
const lib = versionup.bind(versionup)

Object.assign(lib, versionup, {
  versionup
})

module.exports = lib
